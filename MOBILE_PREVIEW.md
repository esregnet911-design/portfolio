# 手机预览本地 Next.js 网站

## 1. 启动开发服务器

```bash
pnpm dev
```

项目已配置为监听 `0.0.0.0`，同一 WiFi 下的手机可以通过电脑局域网 IP 访问。

## 2. 当前电脑局域网地址

本机当前检测到的 WiFi/LAN 地址：

```text
http://192.168.31.155:3000
```

在手机浏览器中打开上面的地址即可预览网站。

## 3. 如果 IP 变化

电脑换 WiFi、重启路由器或网络变化后，IP 可能变化。可在 Mac 终端运行：

```bash
ifconfig | grep "inet "
```

优先使用形如 `192.168.x.x` 或 `10.x.x.x` 的地址，然后加上端口：

```text
http://你的电脑IP:3000
```

## 4. 手机无法访问时检查

- 电脑和手机必须连接同一个 WiFi。
- 手机不要开启蜂窝网络代理或 VPN。
- Mac 第一次运行时如果弹出防火墙提示，请允许 Node.js / Next.js 接收传入连接。
- 确认开发服务器终端里显示 `Network` 地址。
- 如果 3000 端口被占用，可换端口启动：

```bash
pnpm dev -- --port 3001
```

然后手机访问：

```text
http://你的电脑IP:3001
```

## 5. 本机预览

电脑本机仍可访问：

```text
http://127.0.0.1:3000
```
